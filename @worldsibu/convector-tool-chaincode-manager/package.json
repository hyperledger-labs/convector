{
  "name": "@worldsibu/convector-tool-chaincode-manager",
  "version": "1.0.2",
  "description": "Chaincode manager to install and run chaincodes",
  "main": "./dist/src/index.js",
  "typings": "./dist/src/index.d.ts",
  "files": [
    "dist/*"
  ],
  "license": "ISC",
  "bin": {
    "chaincode-manager": "./dist/src/command.js"
  },
  "scripts": {
    "clean": "rimraf dist",
    "clean:docs": "rimraf docs",
    "build": "npm run clean && tsc",
    "prepare": "npm run build",
    "lint": "tslint --fix -c '../../tslint.json' -p './tsconfig.json'",
    "predocs:generate": "npm run clean:docs",
    "docs:generate": "typedoc src --out docs --target ES6 --excludePrivate",
    "docs:serve": "http-server docs"
  },
  "dependencies": {
    "@worldsibu/convector-common-fabric-helper": "^1.0.2",
    "@worldsibu/convector-core-chaincode": "^1.0.2",
    "@worldsibu/convector-core-errors": "^1.0.2",
    "commander": "^2.15.1",
    "docker-composer-manager": "^0.1.3",
    "fabric-ca-client": "^1.1.2",
    "fabric-client": "^1.1.2",
    "fs-extra": "^6.0.1",
    "tslib": "^1.9.0"
  },
  "devDependencies": {
    "npm-scripts-watcher": "^1.0.2",
    "rimraf": "^2.6.2",
    "ts-node": "^6.0.3",
    "tslint": "^5.9.1",
    "typescript": "2.8.3"
  }
}
