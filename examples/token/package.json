{
  "name": "@worldsibu/convector-example-token",
  "version": "0.0.1",
  "description": "Convector Token Example",
  "license": "ISC",
  "main": "./dist/src/index.js",
  "scripts": {
    "clean": "rimraf dist client",
    "build": "npm run clean && npm run client:generate && tsc",
    "lint": "tslint --fix -c '../../tslint.json' -p './tsconfig.json'",
    "test": "mocha -r ts-node/register tests/**/*.spec.ts --reporter spec",
    "client:generate": "generate-controller-interface -c TokenController"
  },
  "dependencies": {
    "@worldsibu/convector-base-storage": "^0.0.2",
    "@worldsibu/convector-chaincode": "^0.0.2",
    "@worldsibu/convector-controller": "^0.0.2",
    "@worldsibu/convector-controller-adapter": "^0.0.2",
    "@worldsibu/convector-model": "^0.0.2",
    "@worldsibu/convector-stub-storage": "^0.0.2",
    "reflect-metadata": "^0.1.12",
    "tslib": "^1.9.0",
    "yup": "^0.24.1"
  },
  "devDependencies": {
    "@worldsibu/convector-mock-controller-adapter": "^0.0.2",
    "mocha": "^5.0.3",
    "rimraf": "^2.6.2",
    "ts-node": "^6.0.3",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}
